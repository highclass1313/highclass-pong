{
  "name": "highclass-pong",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm run test:js && npm run test:contracts",
    "test:js": "jest --coverage",
    "test:contracts": "node scripts/compile.js && hardhat test --no-compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "jest": "^29.7.0"
  }
}
